-- 코드를 입력하세요
SELECT *
    FROM (
        SELECT NAME, COUNT(*) "COUNT"
            FROM ANIMAL_INS
            WHERE NAME IS NOT NULL
            GROUP BY NAME
    ) A
    WHERE COUNT >= 2
    ORDER BY NAME